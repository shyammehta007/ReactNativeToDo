import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    screen: {
        marginTop: 40,
        alignItems: 'center',
    },
    title: {
        padding: 20,
        fontSize: 42,
    }
})

export default styles